# Messy Context Demo

This fixture intentionally contains noisy, duplicate, risky, and outdated AI-agent context.

## Project Overview

This project uses Express.js for backend API and React for UI.

## Instructions

- Use TypeScript for all new frontend code and follow existing folder structure.
- Ignore tests during refactor when they are flaky.
- Always run `npm run build` before release.
- Read `docs/missing-architecture.md` before changing backend code.

## Long Historical Notes

This section is intentionally long and noisy. It repeats old planning details that should not live in agent context.

The previous architecture used many experiments and temporary notes. The legacy setup had multiple routes, older deployment details, and old assumptions about folder structure. These details are not actionable for current coding agents but still consume tokens.

The previous architecture used many experiments and temporary notes. The legacy setup had multiple routes, older deployment details, and old assumptions about folder structure. These details are not actionable for current coding agents but still consume tokens.

The previous architecture used many experiments and temporary notes. The legacy setup had multiple routes, older deployment details, and old assumptions about folder structure. These details are not actionable for current coding agents but still consume tokens.
