# Architecture

This project uses Express.js for backend API.

Frontend lives in `frontend/` and backend lives in `api/`.

Run `npm run deploy` after merging to main.

The previous architecture was temporary and should be updated later.
